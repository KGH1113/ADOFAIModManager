<Page
    x:Class="ADOFAIModManager.Windows.Views.LogsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Padding="32,28,32,20" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Spacing="6">
                <TextBlock Text="로그" Style="{ThemeResource TitleTextBlockStyle}" />
                <TextBlock Text="게임 실행 기록과 UMM 기술 로그를 확인합니다."
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>
            <Button Grid.Column="1" Content="새로 고침" Click="Refresh_Click" VerticalAlignment="Center" />
        </Grid>

        <TabView Grid.Row="1" Margin="24,0,24,24" IsAddTabButtonVisible="False" CanDragTabs="False">
            <TabViewItem Header="게임 로그" IsClosable="False">
                <Grid Padding="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="최근 30줄" FontWeight="SemiBold" VerticalAlignment="Center" />
                        <Button Content="Explorer에서 보기" HorizontalAlignment="Right" Click="OpenGameLog_Click" />
                    </Grid>
                    <TextBox Grid.Row="1" Text="{Binding GameLogText, Mode=OneWay}" IsReadOnly="True"
                             AcceptsReturn="True" TextWrapping="NoWrap" FontFamily="Consolas"
                             ScrollViewer.HorizontalScrollBarVisibility="Auto"
                             ScrollViewer.VerticalScrollBarVisibility="Auto" />
                </Grid>
            </TabViewItem>

            <TabViewItem Header="UMM 로그" IsClosable="False">
                <Grid Padding="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="UMM 기술 로그" FontWeight="SemiBold" VerticalAlignment="Center" />
                        <Button Content="Explorer에서 보기" HorizontalAlignment="Right" Click="OpenUmmLog_Click" />
                    </Grid>
                    <TextBox Grid.Row="1" Text="{Binding UmmLogText, Mode=OneWay}" IsReadOnly="True"
                             AcceptsReturn="True" TextWrapping="NoWrap" FontFamily="Consolas"
                             ScrollViewer.HorizontalScrollBarVisibility="Auto"
                             ScrollViewer.VerticalScrollBarVisibility="Auto" />
                </Grid>
            </TabViewItem>

            <TabViewItem Header="작업 기록" IsClosable="False">
                <ListView ItemsSource="{Binding OperationLogs}" Padding="8">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding DisplayText}" FontFamily="Consolas" TextWrapping="Wrap" />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </TabViewItem>
        </TabView>
    </Grid>
</Page>
