# Windows assets

`AppIcon.ico` is the Windows multi-resolution application icon generated from the
macOS master artwork. Keep this file beside the project so both the executable and
the window use the same icon in published builds.
