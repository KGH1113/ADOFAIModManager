<Window
    x:Class="ADOFAIModManager.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="using:ADOFAIModManager.Windows.Views">

    <Grid x:Name="Root">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid x:Name="TitleBar" Height="48" Padding="16,0" Background="{ThemeResource LayerFillColorDefaultBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                <FontIcon Glyph="&#xE7FC;" FontSize="17" />
                <TextBlock Text="ADOFAI Mod Manager" FontWeight="SemiBold" VerticalAlignment="Center" />
            </StackPanel>
            <Button Grid.Column="1" Click="Settings_Click"
                    AutomationProperties.Name="{Binding Localization[Common_Settings], Mode=OneWay}"
                    ToolTipService.ToolTip="{Binding Localization[Common_Settings], Mode=OneWay}">
                <FontIcon Glyph="&#xE713;" />
            </Button>
        </Grid>

        <NavigationView
            x:Name="Navigation"
            Grid.Row="1"
            IsBackButtonVisible="Collapsed"
            IsSettingsVisible="False"
            IsPaneToggleButtonVisible="True"
            PaneDisplayMode="Left"
            OpenPaneLength="220"
            SelectionChanged="Navigation_SelectionChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="{Binding Localization[Nav_Install], Mode=OneWay}" Tag="install">
                    <NavigationViewItem.Icon><SymbolIcon Symbol="Download" /></NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="{Binding Localization[Nav_Mods], Mode=OneWay}" Tag="mods">
                    <NavigationViewItem.Icon><SymbolIcon Symbol="Library" /></NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="{Binding Localization[Nav_Logs], Mode=OneWay}" Tag="logs">
                    <NavigationViewItem.Icon><SymbolIcon Symbol="Document" /></NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <Grid>
                <Frame x:Name="ContentFrame" />

                <InfoBar
                    Margin="24"
                    VerticalAlignment="Bottom"
                    Severity="Error"
                    IsOpen="{Binding HasError, Mode=OneWay}"
                    Title="{Binding Localization[Error_Title], Mode=OneWay}"
                    Message="{Binding LastError, Mode=OneWay}"
                    CloseButtonClick="ErrorInfoBar_CloseButtonClick" />

                <Border
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    Margin="24"
                    Padding="14,8"
                    CornerRadius="10"
                    Visibility="{Binding BusyVisibility, Mode=OneWay}"
                    Background="{ThemeResource SolidBackgroundFillColorBaseBrush}"
                    BorderBrush="{ThemeResource SurfaceStrokeColorFlyoutBrush}"
                    BorderThickness="1">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <ProgressRing Width="18" Height="18" IsActive="{Binding IsBusy, Mode=OneWay}" />
                        <TextBlock Text="{Binding Activity, Mode=OneWay}" VerticalAlignment="Center" />
                    </StackPanel>
                </Border>
            </Grid>
        </NavigationView>
    </Grid>
</Window>
