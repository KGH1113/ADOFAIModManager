using Microsoft.UI.Xaml.Controls;

namespace ADOFAIModManager.Windows.Views;

public sealed partial class CatalogPage
{
    private StackPanel DescriptionPanel { get; } = new();
    private Image PreviewImage { get; } = new();
    private void InitializeComponent() { }
}
